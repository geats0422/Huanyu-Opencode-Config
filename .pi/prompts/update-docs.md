---
description: "Update documentation to reflect recent code changes"
subagent: worker
---

<!-- Converted from opencode command `update-docs` (delegates to subagent: doc-updater). -->
根据最近代码变更更新文档。

范围: $ARGUMENTS

检查 README、docs、API 文档、示例、JSDoc，确保文档和实现一致。
