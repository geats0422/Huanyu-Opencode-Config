---
description: "Create an implementation plan from approved design doc"
subagent: planner
---

<!-- Converted from opencode command `plan` (delegates to subagent: planner). -->
使用 writing-plans 技能为以下需求创建实施计划。先读取最新设计文档 (docs/designs/)。

$ARGUMENTS
