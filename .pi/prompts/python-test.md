---
description: "Run Python testing workflow and improve pytest coverage"
subagent: worker
---

<!-- Converted from opencode command `python-test` (delegates to subagent: tdd-guide). -->
使用 python-testing 技能执行 Python 测试工作流。

范围: $ARGUMENTS

要求: 运行 pytest，定位失败，按 TDD 补充关键测试，报告覆盖缺口。
