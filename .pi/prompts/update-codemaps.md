---
description: "Update architecture/code map documentation"
subagent: worker
---

<!-- Converted from opencode command `update-codemaps` (delegates to subagent: doc-updater). -->
更新代码地图和架构文档。

范围: $ARGUMENTS

输出模块结构、关键入口、数据流、依赖关系、近期变更摘要。
