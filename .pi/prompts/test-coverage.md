---
description: "Analyze test coverage and propose meaningful missing tests"
subagent: worker
---

<!-- Converted from opencode command `test-coverage` (delegates to subagent: tdd-guide). -->
分析测试覆盖率并识别关键缺口。

范围: $ARGUMENTS

优先关注认证、安全、核心业务逻辑、边界情况。覆盖率是指标，不是目标；生成有意义的测试建议。
